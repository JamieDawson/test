authorization {
    user {
      oauth2-authorization-code (Spotify) {
        authorize-endpoint (https://accounts.spotify.com/authorize)
        client-id (e9595974b8a74f0f8012dbe69d308661)
        client-secret-key (secret)
        scope (user-read-playback-state)
        token-endpoint (https://accounts.spotify.com/api/token)
      }
    }
  }

  //user = signed in
  //gloabal = not signed in info.
  //

//The old way we use to use oauth.
  // authorization{
  //   oauth{
  //     provider(Spotify)
  //     grant-type (authorization_code)
  //     authorize-endpoint (https://accounts.spotify.com/authorize)
  //     client-id () //PUT CLIENT ID HERE
  //     client-secret () //PUT SECRET ID HERE
  //     scope (user-read-playback-state)
  //     token-endpoint (https://accounts.spotify.com/api/token)
  //   }
  // }